{
  "name": "replay-vscode",
  "displayName": "Replay in VSCode",
  "description": "Replay in VSCode",
  "version": "0.0.1",
  "preview": true,
  "private": true,
  "icon": "resources/replay-logo.png",
  "engines": {
    "vscode": "^1.84.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "replayViewContainer",
          "title": "Replay",
          "icon": "resources/replay-logo.png"
        }
      ]
    },
    "views": {
      "replayViewContainer": [
        {
          "id": "replayRecordingsView",
          "name": "Recordings"
        }
      ]
    },
    "commands": [
      {
        "command": "replay-vscode.helloWorld",
        "title": "Hello World"
      }
    ]
  },
  "scripts": {
    "publish-devtools": "yarn install && cd $REPLAY_DIR/devtools && cd packages/design && npx yalc publish --private && cd ../../packages/node-protocol && npx yalc publish --private && cd ../../packages/shared && npx yalc publish --private && cd ../../packages/protocol && npx yalc publish --private && cd ../../packages/replay-next && npx yalc publish --private && cd ../.. && npx yalc publish",
    "linkitzz": "npx yalc link design && npx yalc link node-protocol && npx yalc link shared && npx yalc link protocol && npx yalc link replay-next",
    "linkit": "npx yalc link shared && npx yalc link protocol",
    "vscode:prepublish": "yarn run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "yarn run compile-tests && yarn run compile && yarn run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.3",
    "@types/node": "18.x",
    "@types/vscode": "^1.84.0",
    "@types/ws": "^8",
    "@typescript-eslint/eslint-plugin": "^6.9.0",
    "@typescript-eslint/parser": "^6.9.0",
    "@vscode/test-electron": "^2.3.6",
    "bufferutil": "^4.0.8",
    "eslint": "^8.52.0",
    "glob": "^10.3.10",
    "mocha": "^10.2.0",
    "ts-loader": "^9.5.0",
    "tsconfig-paths-webpack-plugin": "^4.1.0",
    "typescript": "^5.2.2",
    "utf-8-validate": "^6.0.3",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@dbux/common": "^0.7.9",
    "@replayio/protocol": "^0.63.0",
    "@replayio/replay": "^0.18.0",
    "open": "^9.1.0",
    "strip-color": "^0.1.0",
    "tseep": "^1.1.3",
    "ws": "^8.14.2"
  },
  "packageManager": "yarn@4.0.1"
}
